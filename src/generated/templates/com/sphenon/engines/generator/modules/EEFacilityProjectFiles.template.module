G-2.0-plain_java-1.0
▼ import com.sphenon.assemblies.emos.EMOSFacilityOptions; ▼…
▼ import com.sphenon.basics.encoding.Encoding; ▼…
⊏ signature(EMOSFacilityOptions options) ⊐…
«
    object = options;
    String root = options.getRoot(context);
    String projectid = options.getProjectId(context);
    String facilityid = options.getFacilityId(context);
    String facilitypath = Encoding.recode(context, options.getFacilityId(context), Encoding.DSP, Encoding.SSP);
»…
